BBR can back up the following components:

* BOSH Director
* {{  vars.product }} control plane API VM and its ETCD database
* {{  vars.product }} control plane database VM (MySQL)
* {{  vars.product }} cluster data, from the clusters' ETCD databases
