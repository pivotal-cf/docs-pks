  On the command line, run the following command to log in:
    <pre>
    pks login -a PKS-API -u USERNAME -k
    </pre>
    Where:
    * `PKS-API` is the domain name for the PKS API that you entered in **Ops Manager** > **<%= vars.product_tile %>** > **PKS API** > **API Hostname (FQDN)**.
    For example, `api.pks.example.com`.
    * `USERNAME` is your user name.
<br><br>
    See [Logging in to <%= vars.product_short %>](login.html) for more information about the `pks login` command.
    <%= partial "saml-sso-login" %>
