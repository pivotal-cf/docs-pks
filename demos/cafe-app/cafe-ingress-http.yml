apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cafe-ingress
spec:
  rules:
  - host: cafe.example.com
    http:
      paths:
      - path: /tea
        pathType: Prefix
        backend:
          serviceName: 
            name: tea-svc
            port: 
              number: 80
      - path: /coffee
        pathType: Prefix
        backend:
          serviceName: 
            name: coffee-svc
            port: 
              number: 80