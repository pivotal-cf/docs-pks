---
title: Prerequisites for a Flannel Network
owner: TKGI
---

You can select the option for <%= vars.product_short %> Management Console to provision a Flannel container networking interface for you during <%= vars.product_short %> deployment on vSphere.

Obtain the following IP addresses to use for deployment to a Flannel network:

- DNS server, subnet, subnet mask, and gateway of the network on which to deploy <%= vars.product_short %>
- DNS server, subnet, subnet mask, and gateway of the Flannel service network
- Subnet range and subnet mask for the Kubernetes pod and Kubernetes service networks