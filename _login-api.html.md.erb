  On the command line, run the following command to log in:
    <pre>
    tkgi login -a TKGI-API -u USERNAME -k
    </pre>
    Where:
    * `TKGI-API` is the domain name for the <%= vars.control_plane %> that you entered in **Ops Manager** > **<%= vars.product_tile %>** > **<%= vars.control_plane %>** > **API Hostname (FQDN)**.
    For example, `api.tkgi.example.com`.
    * `USERNAME` is your user name.
<br><br>
    See [Logging in to <%= vars.product_short %>](login.html) for more information about the `tkgi login` command.
    <%= partial "saml-sso-login" %>
